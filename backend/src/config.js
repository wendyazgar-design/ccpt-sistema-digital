import dotenv from 'dotenv';
dotenv.config();
const isProduction = process.env.NODE_ENV === 'production';
const requiredInProduction = ['JWT_SECRET','DATABASE_URL','FRONTEND_ORIGIN','STORAGE_MODE','S3_BUCKET','S3_REGION','S3_ACCESS_KEY_ID','S3_SECRET_ACCESS_KEY'];
if (isProduction) { const missing=requiredInProduction.filter(k=>!process.env[k]); if(missing.length) throw new Error(`Faltan variables de producción: ${missing.join(', ')}`); if(process.env.STORAGE_MODE !== 's3') throw new Error('Producción requiere STORAGE_MODE=s3'); }
export const config={nodeEnv:process.env.NODE_ENV||'development',isProduction,port:Number(process.env.PORT||4000),jwtSecret:process.env.JWT_SECRET||'dev-only-change-me',frontendOrigin:process.env.FRONTEND_ORIGIN||'http://localhost:5173',databaseUrl:process.env.DATABASE_URL||'',db:{host:process.env.DB_HOST||'localhost',port:Number(process.env.DB_PORT||5432),database:process.env.DB_NAME||'ccpt',user:process.env.DB_USER||'postgres',password:process.env.DB_PASSWORD||'postgres'},storageMode:process.env.STORAGE_MODE||'local',uploadDir:process.env.UPLOAD_DIR||'uploads',maxFileBytes:Number(process.env.MAX_FILE_BYTES||10*1024*1024),s3:{bucket:process.env.S3_BUCKET||'',region:process.env.S3_REGION||'us-east-1',endpoint:process.env.S3_ENDPOINT||'',accessKeyId:process.env.S3_ACCESS_KEY_ID||'',secretAccessKey:process.env.S3_SECRET_ACCESS_KEY||'',sse:process.env.S3_SSE||'AES256'}};
