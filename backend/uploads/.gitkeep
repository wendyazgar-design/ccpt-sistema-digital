# No expongas esta carpeta mediante express.static.
# En producción, monta un volumen persistente o configura S3/Cloudinary privado.
# Los documentos se sirven únicamente por /api/documents/:expedienteId/:documentId/download.
